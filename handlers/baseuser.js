var order=require('../models/orders')
var shop=require('../models/shops')
/* retrieve the latest document  from orders schema and  shop name from shop schema and send them alongwith route to track order*/
